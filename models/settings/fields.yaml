fields:
    blog_title:
        label: Blog Title
        description: The title for this blog

    tables:
        label: Tables to Sync
        description: What tables will automatically sync
        type: checkboxlist

    columns:
        label: Columns to Sync
        description: More granular sync settings
        type: checkboxlist
        dependsOn: tables
